import { Input } from "@/components/ui/input"
import React from "react"

type Props = {}

const SDate = (props: Props) => {
  return <Input placeholder="Enter your question here" />
}

export default SDate